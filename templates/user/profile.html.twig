{% extends 'base.html.twig' %}
   
{% block title %}Mon compte - {{ parent() }}{% endblock %}

{% block stylesheets %}
    	{{ encore_entry_link_tags('page_profile') }}
{% endblock %}

   {% block body %}
    
   <div class="container-fluid h-100">
    <div class="row h-100">
      <aside class="col-12 col-md-2 p-0">
        <nav class="navbar navbar-expand navbar-light bg-light flex-md-column flex-row align-items-start">
          <div class="collapse navbar-collapse">
            <ul class="flex-md-column flex-row navbar-nav w-100 justify-content-between">
                <li class="nav-item">
                    <a class="nav-link pl-0" href="#">Mes annonces</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link pl-0" href="#">Mes messages</a>
                </li>
                        
            </ul>
          </div>
        </nav>
      </aside>


      <main class="col">
        <div class="container">

            <div class=""><h2>Profil de {{ app.user.firstname }} {{ app.user.lastname }}</h2></div>

            <div class="">Nom</div>
            <div class="">{{ app.user.lastname }}</div>
            
            <div class="">Prénom</div>
            <div class="">{{ app.user.firstname }}</div>

            <div class="">Nom d'utilisateur</div>
            <div class="">{{ app.user.username }}</div>

            <div class="">Email</div>
            <div class="">{{ app.user.email }}</div>

            <br>
            
            <a href="" class="btn btn-secondary btn-lg">Modifier le profil</a>
            <a href="" class="btn btn-secondary btn-lg">Modifier le mot de passe</a>        

        </div>
      </main>
    </div>
  </div>

  <hr>
    {% endblock %}



{% block javascripts %}
    {{ encore_entry_script_tags('page_profile') }}
{% endblock %}